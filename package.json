{
  "name": "mandarin-tutor",
  "version": "1.0.0",
  "private": true,
  "description": "A web app for leveling up Mandarin speaking & listening (intermediate → high-school), powered by Claude + the browser Web Speech API.",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
